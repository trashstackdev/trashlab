{{- if and (not .Values.rails.existingSecret) .Values.rails.secret.enabled }}
{{- fail "Inline secret values are not allowed. Set rails.existingSecret to reference an existing Secret (e.g. managed by External Secrets Operator)." }}
{{- end }}
